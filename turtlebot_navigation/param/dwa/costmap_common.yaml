# footprint: [[-0.5, -0.33], [-0.5, 0.33], [0.5, 0.33], [0.5, -0.33]]
# footprint_padding: 0.01

# robot_base_frame: base_link
# update_frequency: 4.0
# publish_frequency: 3.0
# transform_tolerance: 0.5

# resolution: 0.05

# obstacle_range: 5.5
# raytrace_range: 6.0

# #layer definitions
# static:
#     map_topic: /map
#     subscribe_to_updates: true

# obstacles_laser:
#     observation_sources: laser
#     laser: {data_type: LaserScan, clearing: true, marking: true, topic: realsense/scan, inf_is_valid: true}

# inflation:
#     inflation_radius: 0.2

#---standard pioneer footprint---
#---(in meters)---
#robot_radius: 0.17
#footprint: [[-0.25, -0.25], [-0.25, 0.25], [0.25, 0.25], [0.25, -0.25]]
#footprint_padding: 0.00

transform_tolerance: 0.5
map_type: costmap

# always_send_full_costmap: true

robot_radius: 0.2
resolution: 0.05

obstacle_layer:
  enabled: true
  obstacle_range: 6.0
  raytrace_range: 6.0
  inflation_radius: 0.4
  track_unknown_space: true
  combination_method: 1

  observation_sources: laser
  laser: {data_type: LaserScan, topic: scan, marking: true, clearing: true, inf_is_valid: true}

obstacles_laser:
    observation_sources: laser
    laser: {data_type: LaserScan, clearing: true, marking: true, topic: scan, inf_is_valid: true}


inflation_layer:
  enabled:              true
  cost_scaling_factor:  10.0  # exponential rate at which the obstacle cost drops off (default: 10)
  inflation_radius:     0.4  # max. distance from an obstacle at which costs are incurred for planning paths.

# static_layer:
#   enabled:              true
#   map_topic:            "/map"
